[Unit]
Description=Flask Web Application
After=network.target

[Service]
ExecStart=/usr/bin/python3 -m flask run --host=0.0.0.0
WorkingDirectory=/DevOps-Crypto
Environment=FLASK_ENV=production

[Install]
WantedBy=multi-user.target
